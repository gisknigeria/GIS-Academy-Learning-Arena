{
  "name": "gis-konsult-knowledge-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "dev:api": "npm --workspace apps/api run dev",
    "build": "npm --workspace apps/web run build && npm --workspace apps/api run build",
    "build:web": "npm --workspace apps/web run build",
    "build:api": "npm --workspace apps/api run build",
    "preview:web": "npm --workspace apps/web run preview",
    "prisma:generate": "npm --workspace apps/api run prisma:generate",
    "prisma:migrate": "npm --workspace apps/api run prisma:migrate",
    "prisma:migrate:deploy": "npm --workspace apps/api run prisma:migrate:deploy",
    "prisma:studio": "npm --workspace apps/api run prisma:studio",
    "prisma:seed": "npm --workspace apps/api run prisma:seed"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0",
    "typescript": "^5.8.0"
  },
  "devDependencies": {}
}
